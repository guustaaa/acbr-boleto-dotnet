# Security policy

Do not report credentials or customer data in a public issue. If you find a
secret in the repository, contact the maintainer privately and rotate the
credential immediately.

Bank OAuth credentials, certificates, taxpayer identifiers, generated INI
files, native logs, and boleto payloads must remain outside version control.
Tests in this public edition use placeholders only.
